---

devices:
  example.host.tld:
    username: monitoring
    password: <redacted>
    enabled_collectors:
      - optics
      - cpu
      - bgp
      - memory
      - environment
      - nat
      - interfaces
      - aaa
      - users
